import { Button, Card } from '@/components';

function App() {
  return (
    <main>
      <Button variant={'primary'} aria-label={'Hello'}>
        Button
      </Button>
      <Card />
    </main>
  );
}

export default App;
